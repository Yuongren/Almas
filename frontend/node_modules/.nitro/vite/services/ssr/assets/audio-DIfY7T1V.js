import { s as supabase } from "./client-CT4naPST.js";
const AUDIO_CATEGORIES = [
  { value: "campaign_tunes", label: "Campaign Tunes" },
  { value: "church_audio", label: "Church Audio" },
  { value: "native_languages", label: "Native Languages" },
  { value: "business_greetings", label: "Business Greetings" },
  { value: "hold_music", label: "Hold Music" },
  { value: "voice_overs", label: "Voice Overs" }
];
const AUDIO_CATEGORY_VALUES = AUDIO_CATEGORIES.map((category) => category.value);
function categoryLabel(v) {
  return AUDIO_CATEGORIES.find((c) => c.value === v)?.label ?? v;
}
async function fetchTracks() {
  const { data, error } = await supabase.from("audio_tracks").select("id, title, description, category, storage_path, is_paid, skiza_code, created_at").order("created_at", { ascending: false });
  if (error) {
    throw new Error(error.message || "Unable to load audio library");
  }
  return (data ?? []).map((row) => ({
    id: row.id,
    title: row.title,
    description: row.description ?? null,
    category: row.category,
    storage_path: row.storage_path,
    is_paid: row.is_paid,
    skiza_code: row.skiza_code ?? null,
    created_at: row.created_at
  }));
}
async function fetchRatings() {
  const { data, error } = await supabase.from("track_ratings").select("track_id, rating");
  if (error) {
    throw new Error(error.message || "Unable to load ratings");
  }
  const grouped = /* @__PURE__ */ new Map();
  for (const row of data ?? []) {
    const current = grouped.get(row.track_id) ?? { sum: 0, count: 0 };
    current.sum += row.rating;
    current.count += 1;
    grouped.set(row.track_id, current);
  }
  return Object.fromEntries(
    Array.from(grouped.entries()).map(([trackId, value]) => [
      trackId,
      { avg: value.sum / value.count, count: value.count }
    ])
  );
}
async function fetchMyRatings(userId) {
  const { data, error } = await supabase.from("track_ratings").select("track_id, rating").eq("user_id", userId);
  if (error) {
    throw new Error(error.message || "Unable to load your ratings");
  }
  return Object.fromEntries((data ?? []).map((row) => [row.track_id, row.rating]));
}
async function rateTrack(trackId, rating, userId) {
  const { data: existing, error: existingError } = await supabase.from("track_ratings").select("id").eq("track_id", trackId).eq("user_id", userId).maybeSingle();
  if (existingError) {
    throw new Error(existingError.message || "Unable to update rating");
  }
  if (existing) {
    const { error: error2 } = await supabase.from("track_ratings").update({ rating }).eq("id", existing.id);
    if (error2) {
      throw new Error(error2.message || "Unable to update rating");
    }
    return;
  }
  const { error } = await supabase.from("track_ratings").insert({ track_id: trackId, user_id: userId, rating });
  if (error) {
    throw new Error(error.message || "Unable to create rating");
  }
}
export {
  AUDIO_CATEGORY_VALUES as A,
  fetchRatings as a,
  fetchMyRatings as b,
  categoryLabel as c,
  AUDIO_CATEGORIES as d,
  fetchTracks as f,
  rateTrack as r
};
